## アセット

- `/torifuda` フォルダ内に、取り札の画像ファイルが PNG 形式で入っている
    - `tori_ura.png` は裏向きになった取り札の画像
- `fudalist.js` に、100首の歌の情報が入っている
    - `no`：歌番号
    - `kimariji`：決まり字
    - `normal`：取り札の正位置（自陣）での向きの画像ファイルへのパス
    - `reverse`：取り札の逆位置（相手陣）での向きの画像ファイルへのパス
    - `order`：定位置編集画面の未配置プールの並び順、および確認モード「決まり字順」のソートキー

## ドキュメント一覧

| ファイル | 内容 |
| --- | --- |
| `0.概要.md` | 目的・ターゲット・Phase 1/2 スコープ |
| `1.用語説明.md` | 陣地・配置・用語 |
| `2.要件定義.md` | 機能・非機能要件 |
| `3.画面仕様.md` | 画面ごとの UI・挙動 |
| `4.技術・データ仕様.md` | スタック・データ・PWA・配置アルゴリズム |
| `9.補足情報.md` | 本ファイル（アセット・実装状況） |

## Web アプリ（リポジトリ構成）

- ルートに Vite + React + TypeScript プロジェクト（`npm run dev`）
- `src/data/fudalist.ts` … 歌データ（`npm run generate:fudalist` で `fudalist.js` から再生成）
- `public/torifuda` … `torifuda/` へのシンボリックリンク（静的配信）
- 主要ソース：`src/pages/`（画面）、`src/lib/`（配置・確認・定位置・効果音）

## Phase 1 実装状況（コードと同期）

以下は **実装済み**。仕様の詳細は各 doc を参照。

- 定位置 v2（LocalStorage キー `position`、旧 `teigi` からの移行）
- 定位置編集：dnd-kit、コンパクトテキスト `# position v2`
- 暗記スタート：2列グリッド設定、デフォルト値（相手陣25枚・5分・空札15枚など）
- 確認モード：近接正解、○（CSS円）/×/△オーバーレイ、決まり字の色フィードバック
- トップ：成績表示（日時は秒なし）、URLコピー、作者リンク
- PWA アイコン（180/192/512）

## 未実装・未確定（Phase 2）

| 項目 | 状態 |
| --- | --- |
| モバイル陣ズーム | **未実装**。ピンチ/ボタン/パンの具体仕様は未確定（`2.要件定義.md` の例を参照） |
| 効果音 ON/OFF | 仕様外（未実装） |
| 成績のエクスポート・5回超の保存 | 仕様外（未実装） |
| JSON 形式の定位置インポート | 意図的に非対応（テキストのみ） |
